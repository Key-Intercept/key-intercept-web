import { useState } from "react";
import type { Rule } from "../script/types";

export default function SelectedRule({selectedRule, setRule}: {selectedRule: Rule, setRule: (rule: Rule) => void }) {
    
}